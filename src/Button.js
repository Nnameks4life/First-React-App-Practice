import React from "react";

const Button = () => {
  return (
    <div>
      <h1>This is my first react component</h1>
      <button className="btn bg-btn-primary">clickMe</button>
    </div>
  );
};

export default Button;
